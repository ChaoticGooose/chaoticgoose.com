---
title: "Home"
layout: main
---

<ul>
    {% for post in collections.post reversed %}
    <li><a href="{{post.url}}">{{post.data.title}}</a> - {{post.date}}</li>
    {% excerpt post %}
    {%endfor%}
</ul>

